# Debug Page for Exercise (Generation)

Make a dedicated page where we can edit a unit of meaning (implies basic for for that), and based on that we can see what happens if we generate different exercises.